name: API Lint

on:
  push:
  workflow_dispatch:

jobs:
  job:
    name: API Lint
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: API Lint
        run: make api-lint
