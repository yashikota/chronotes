get:
  operationId: getTagAnalytics
  summary: Get tag usage analytics
  parameters: []
  responses:
    '200':
      description: The request has succeeded.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/GetTagAnalyticsResponse.yaml
          example: {}
    default:
      description: An unexpected error response.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
  tags:
    - Analytics
  security:
    - BearerAuth: []
